<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :static_pages, action: :home } %>
<% elsif params[:search].present? %>
  <p>「<%= params[:search] %>」に一致するポストはありません。</p>
<% end %>
